<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.devstudio.mlabsa.accom" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Entry for RegisterActivity.class -->
        <activity
            android:name=".RegisterActivity"
            android:label="Register NEW ACCOUNT" >
            </activity>
        <activity
            android:name=".MenuActivity"
            android:label="select MENU">
            </activity>
        <activity
            android:name=".ListActivity"
            android:label="view list">
            </activity>
        <activity
            android:name=".RegisterAdActivity"
            android:label="register an AD">
            </activity>
        <provider
            android:name=".provider.AccommodationContentProvider"
            android:authorities="com.devstudio.mlabsa.accom.provider" />


    </application>

</manifest>
